apiVersion: v1
kind: Service
metadata:
  name: industrial-defect-api-service

spec:
  selector:
    app: industrial-defect-api

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000

  type: LoadBalancer